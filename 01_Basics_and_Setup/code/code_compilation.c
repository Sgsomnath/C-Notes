#include <stdio.h> // Header file inclusion

// Preprocessing step replaces macros and expands header files
#define MAX 100

int main() {
    printf("Max value: %d\n", MAX);
    return 0; // Compilation, Assembly, and Linking build the final executable
}